@page "/Auth/ForgetPassword"
@using System.Security.Claims
@using Microsoft.AspNetCore.Authorization

  @attribute [Authorize]
  
  
  <AuthorizeView>
      <Authorized>
          @context.User.FindFirst(ClaimTypes.Email)
      </Authorized>
  </AuthorizeView>

  <MudContainer Class="page-wrapper auth-page-wrapper">

    <!-- Page Content -->
    <MudMainContent Class="page-content">
        <MudContainer>
            <MudGrid Class="auth-wrapper">
                <MudItem xs="12" sm="8" md="6" lg="4" Class="mx-auto">
                    <MudPaper Class="auth-form" Elevation="4" Rounded="true">
                        <MudContainer Class="site-logo">
                            <MudLink Href="#">
                                <MudImage Src="./assets/images/logo.png" Alt="logo"/>
                            </MudLink>
                        </MudContainer>
                        @* <MudText Typo="Typo.h5" Class="auth-form-title">یادآوری کلمه عبور</MudText> *@
                        @* <MudForm> *@
                        @*     <MudItem Class="form-element-row"> *@
                        @*         <MudText Typo="Typo.subtitle2" Class="label-element">شماره موبایل</MudText> *@
                        @*         <MudTextField @bind-Value="phoneNumber" Label="شماره موبایل" Class="input-element" *@
                        @*                       Adornment="Adornment.Start"> *@
                        @*             <AdornmentStart> *@
                        @*                 <MudIcon Icon="fas fa-mobile-alt"></MudIcon> *@
                        @*             </AdornmentStart> *@
                        @*         </MudTextField> *@
                        @*     </MudItem> *@
                        @*     <MudItem Class="form-element-row text-left"> *@
                        @*         <MudButton Variant="Variant.Filled" Color="Color.Success" Class="btn-element btn-success-element"> *@
                        @*             <MudIcon Icon="fas fa-sync"></MudIcon> *@
                        @*             یادآوری کلمه عبور *@
                        @*         </MudButton> *@
                        @*     </MudItem> *@
                        @* </MudForm> *@
                        <MudContainer Class="auth-form-footer">
                            <MudText>کاربر جدید هستید؟</MudText>
                            <MudLink Href="#" Class="link--with-border-bottom">ثبت نام در همتا</MudLink>
                        </MudContainer>
                    </MudPaper>
                </MudItem>
            </MudGrid>
        </MudContainer>
    </MudMainContent>
</MudContainer>
@code {
  

}